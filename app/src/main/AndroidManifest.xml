<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SBuddy"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
             <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.scoring.ScoreActivity" />
        <activity android:name=".ui.scoring.MatchSetupActivity" />
        <activity android:name=".ui.history.MatchHistoryActivity" />
        <activity
            android:name=".ui.tournament.TournamentActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ui.tournament.PublicTournamentsActivity" />
        <activity android:name=".ui.tournament.TournamentDetailActivity" />
        <activity android:name=".ui.group.BuddyGroupActivity" />
        <activity android:name=".ui.group.GroupDetailActivity" />
        <activity android:name=".ui.profile.UserProfileActivity" />
        <activity android:name=".ui.login.SignUpActivity" />
        <activity android:name=".MainActivity" />

    </application>

</manifest>
